package example;

import java.awt.image.BufferedImage;
import engine.game.Mob;

public class Player extends Mob {

	public Player(BufferedImage[] images, String name, int x, int y, int width, int height) {
		super(images, name, x, y, width, height);
	}
	
}
